version: 2
updates:
  # GitHub Actions used in workflows (e.g. CodeQL, CI)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"
